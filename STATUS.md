# 当前状态

更新时间：2026-09-08

## 当前目标

用 DeepSeek API 对复杂问题做结构化拆分，并与不拆分的混合检索基线进行同题对照。

## 已验证

- 工作目录存在，当前原始输入为 `文档平台用测试文档.zip`。
- 压缩包共有 28 个文件条目；排除 1 个 `~$` 临时锁文件后，有效资料 27 份。
- 有效资料包括 24 个 `.docx` 和 3 个 `.doc`，覆盖 M1212、MI012、W001 三组产品。
- 压缩包 CRC 校验未发现损坏条目。

## 本阶段结果

- 已在 `data/raw/20260907/` 建立 27 份只读原文快照，未修改原压缩包。
- 已生成 `data/corpus_manifest.csv`、`data/corpus_manifest.json` 和可审阅的语料清单工作簿。
- 6 份代表文档的正文、标题和表格结构解析全部通过，未发现乱码替换字符。
- 2 份旧版 `.doc` 已成功转换为 `.docx`，转换文件保存在 `_staging/20260907_语料基线/`。
- 已检查 6 份文档的封面和第 5 页；详细结果见 `reports/parse_check.md`。
- 已在 `data/processed/sanitized/` 生成 6 份可重复生成的脱敏入库副本。
- 单位名称、签署信息、文档编号、封面内部代码、账号、密码和日历日期共替换 93 次，自动风险扫描为 0 项。
- 已确认原始 27 份资料哈希与清单一致，且仍为只读；原压缩包哈希未变化。
- 已建立 15 道种子评测题，其中 6 道单文档事实题、3 道跨文档比较题、1 道一致性检查题、1 道版本更新题、2 道证据不足拒答题和 2 道权限隔离题。
- 15 道题均不依赖图片语义；人工复核工作簿已生成。
- 已从 6 份脱敏 Markdown 生成 605 个检索切块，每块保留来源 ID、产品组、文档类型、章节、段落号、Markdown 行号和 ACL。
- 已使用 BM25S 0.3.11 与 jieba 0.42.1 建立可持久化的关键词索引，并验证索引可由独立进程重新加载和检索。
- 15 道种子题的 Top 5 严格评测通过 11 道（73%）；预期来源覆盖率 95%，预期定位覆盖率 77%，MRR 为 0.9286。
- Q014 的 `external-partner` 模拟用户在执行检索前被拦截，未返回文档内容；Q015 的 `fde-core` 用户可正常召回证据。
- 原压缩包哈希未变化，27 份 `data/raw/` 原文哈希全部与清单一致。
- 已初始化本地 Git 仓库，主分支为 `main`；代码、配置、评测集和 Markdown 结论进入版本边界，原文、虚拟环境、模型缓存和可重建索引默认忽略。
- 已用 `pyproject.toml` 统一依赖，并用 `scripts/kb.ps1` 提供环境安装、模型下载、BM25S 和向量基线的一键入口。
- 已安装并导入验证 PyTorch 2.14.0 CPU、Sentence Transformers 6.0.1 和 Qdrant Client 1.19.0。
- Hugging Face 大文件下载失败后，已从 ModelScope 获取 BGE-M3 稠密检索所需文件；未下载重复的 ONNX 权重。
- 本机 CPU 将 605 个切块转换为 1024 维向量并全部写入本地 Qdrant，向量化耗时约 7 分 20 秒。
- BGE-M3 + Qdrant 的 15 道题严格通过 12 道（80%）；来源覆盖率 95%，定位覆盖率 86%，MRR 0.9167。
- 向量基线通过了 BM25S 未通过的 Q009、Q010、Q011；Q007、Q008、Q013 仍未通过。详细结果见 `reports/vector_baseline_eval.md`。
- Git 首次提交已推送到公开仓库 `blackq166s-web/team-knowledge-hub`，本地 `main` 跟踪 `origin/main`；远程文件树复查未发现原文、模型、向量库、虚拟环境或 `.env`。
- 已实现 `src/hybrid_baseline.py`：两路各取 20 个候选，按 `chunk_id` 去重，使用等权 RRF（常数 60）融合，并保留检索前 ACL 与范围过滤。
- RRF 混合召回 Top 5 严格通过 11/15（73%），来源覆盖 95%，定位覆盖 82%；没有超过 80% 的向量单路基线。
- RRF 混合召回 Top 10 严格通过 13/15（87%），来源覆盖 100%，定位覆盖 91%；仍未通过 Q008、Q011。
- 已验证向量优先的 1:2 权重仍只有 11/15，因此没有把针对当前小评测集的调权作为默认配置。
- Q014 无权用户在混合检索前被拦截；详细结果与失败解释见 `reports/hybrid_baseline_eval.md`。
- 已从 ModelScope 下载 `bge-reranker-v2-m3` 必需模型文件到本机 `.cache`；模型文件未进入 Git。
- 已实现 `src/rerank_baseline.py`：混合召回 Top 10 后对问题与候选逐对打分，再返回 Top 5；最大输入 512 tokens。
- Rerank Top 5 严格通过 12/15（80%），来源覆盖 95%，定位覆盖 86%，MRR 0.9167；未超过 BGE-M3 向量单路基线。
- 未通过 Q008、Q011、Q013：前两题缺少完整候选证据，Q013 的所需证据被重排降出 Top 5。
- 本机 CPU 的平均重排耗时为 13.84 秒/题，P95 为 17.12 秒/题，不含首次模型加载。
- batch size 4 的完整对照平均 14.57 秒、P95 19.59 秒，比 batch size 1 更慢，因此默认保持 1。
- Q014 无权用户在召回前被拦截，没有向 Rerank 传入文档内容。详细结果见 `reports/rerank_baseline_eval.md`。
- 已实现 `src/deepseek_decomposition.py`：本地先判断是否需要拆分，再通过 DeepSeek JSON Output 生成 2 至 5 个子问题；默认使用 `deepseek-v4-flash`。
- 已实现 `src/decomposed_hybrid_eval.py`：保留原问题并行检索，为每个有效子问题预留一个证据位，再通过 RRF 补齐 Top 5。
- DeepSeek 只接收用户问题和允许识别的产品名，不接收知识库正文；无权限用户在调用 API 前被拦截。
- 10 个拆分、范围继承和输出校验单元测试已通过；API 缺失时的 15 题自动回退路径也已跑通，权限隔离继续通过。
- 已完成真实 DeepSeek 全量评测：8 次 API 请求成功，其中 6 道题实际拆分；Top 5 严格通过 15/15，来源覆盖率和定位覆盖率均为 100%，MRR 0.9643。
- DeepSeek 判断与拆分平均耗时 3.90 秒，P95 4.22 秒；测试后临时环境变量已删除，密钥未写入项目文件。
- 本轮改进来自“AI 拆分 + 子问题继承原范围 + 无答案值的文档结构词增强”，不能把 15/15 单独归因于模型。详细边界见 `reports/deepseek_decomposition_check.md`。

## 尚未验证

- 脱敏结果的人工逐字审计；当前只通过规则扫描和已知敏感串复查。
- 其余 21 份文档的解析质量。
- 图片中的接线关系、实物标注和流程图语义。
- 大模型回答、引用、真实权限和更新删除链路。
- 真实业务问题上的 RAG 效果。
- BM25S 基线仍有 Q008、Q009、Q010、Q011 四道题未严格通过，集中在跨文档比较、冲突证据和封面表格召回。
- 当前 ACL 仅为语料统一标记 `fde-core` 的 PoC 夹具，尚未接入真实用户、组织和文档权限。
- RRF 只改善候选覆盖，没有直接改善 Top 5；Rerank 已验证但没有超过向量单路基线。
- Rerank 已验证但未达到 13/15 目标；当前 CPU 延迟也不适合直接作为团队在线服务性能结论。
- DeepSeek 本轮只在 15 道已知种子题上验证，尚未完成新增真实问题的盲测、重复稳定性和 token 成本统计。

## 下一步

1. 人工抽查六份脱敏副本和 15 道评测题，确认资料允许进入私有 Git 或团队存储的边界。
2. 新增并冻结 15 至 35 道未参与调参的真实业务题，重复运行拆分评测，验证泛化与稳定性。
3. 在报告中记录 DeepSeek token usage 和调用成本，并为超时、限流和空响应补充专项测试。
4. 暂不把慢速 CPU Rerank 放入默认在线链路；先接 DeepSeek 完成“仅依据证据回答、带引用、证据不足拒答”。
5. 接入真实用户和文档权限后重新验证 Q014、Q015，并测试文档更新与删除。
