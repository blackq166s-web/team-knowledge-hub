# 工作规则

- 开始工作前先阅读 `README.md` 和 `STATUS.md`。
- 原压缩包和 `data/raw/` 是只读事实源，不覆盖、不重命名、不删除。
- AI 草稿、格式转换和预览文件放入 `_staging/YYYYMMDD_事项/`。
- `data/processed/` 只保存可由 `src/` 脚本重复生成的结果。
- 结论必须区分“已验证、待验证、建议”，不能把解析成功当作 RAG 或业务验收。
- 仅在验证完成后更新 `STATUS.md`；失败项和剩余边界必须保留。
- 日志和报告不复制整篇文档正文，不向外部服务上传语料。
